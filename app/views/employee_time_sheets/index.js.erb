<% if @result.present? %>
alert("<%= @result %>")
<% else %>
dataTable.destroy()
$('#timesheet-table').html("<%= j render 'time_sheet_table_data' %>")
dataTable = $('#table').DataTable({
    "ordering": true,
    "lengthMenu": [25, 50, 100, 200],
    "pageLength": 200
});
<% end %>
