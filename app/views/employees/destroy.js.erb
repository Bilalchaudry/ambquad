<% if @destroy %>
var client_companies_path = "/projects/" + <%= @project.id %> + "/employees"
location.href = client_companies_path
alert("Employee deleted successfully")
<% else %>
alert("Employee can not deleted because Employee is linked with associative records.")

<% end %>