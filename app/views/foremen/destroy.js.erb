<% if @destroy %>
var client_companies_path = "/projects/" + <%= @project.id %> +"/foremen"
location.href = client_companies_path
alert("Foreman deleted successfully")
<% else %>
alert("Foreman can not deleted because Foreman is linked with associative records..")

<% end %>