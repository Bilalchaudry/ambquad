<div id="layoutSidenav">
  <div id="layoutSidenav_content">
    <main>
      <div class="container-fluid">
        <!--        <h1 class="mt-3">History</h1>-->

        <div class="card mb-4">
          <div class="card-body">
            <div class="table-responsive">
              <table class="table table-bordered" width="100%" cellspacing="0">
                <thead>
                <tr>
                  <th>User</th>
                  <th>Changes</th>
                  <th>Action</th>
                  <th>Time</th>
                </tr>
                </thead>

                <tbody>
                <% @audits.each do |audit| %>
                  <tr>
                    <td><%= audit.user.username rescue "N/A" %></td>
                    <td><%= audit.audited_changes rescue nil %></td>
                    <td><%= audit.action rescue "N/A" %></td>
                    <td><%= audit.created_at rescue "N/A" %></td>
                  </tr>
                <% end %>
                </tbody>
              </table>
              <%= will_paginate @audits %>
              <br>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>
</div>