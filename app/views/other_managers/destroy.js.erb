<% if @destroy %>
var client_companies_path = "/projects/" + <%= @project.id %> +"/other_managers"
location.href = client_companies_path
alert("Other Manager deleted successfully")
<% else %>
alert("Other Manager can not deleted because Company is linked with Project.")

<% end %>