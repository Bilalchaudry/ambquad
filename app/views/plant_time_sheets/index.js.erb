dataTable.destroy()
$('#timesheet-table').html("<%= j render 'plant_time_sheet_table_data' %>")
dataTable = $('#dataTable').DataTable({
    "ordering": true,
    "scrollY": "500px",
    "scrollX": "700px",
    "scrollCollapse": true,
    "pageLength": 200,
    "lengthMenu": [[50, 100, 200, -1], [50, 100, 200, "All"]]
});
initiateFocusEvents();