<% if @destroy %>
var client_companies_path = "/projects/" + <%= @project.id %> +"/plant_types"
location.href = client_companies_path
alert("Plant Type deleted successfully")
<% else %>
alert("Plant Type can not deleted because Company is linked with Project.")

<% end %>