<table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
  <thead>
  <tr>
    <th>Name</th>
    <th>Phone Number</th>
    <th>Address</th>
    <th>Users</th>
    <th>Actions</th>
  </tr>
  </thead>

  <tbody>
  <% ClientCompany.all.each do |company| %>
    <tr>
      <td><%= company.company_name %></td>
      <td><%= company.phone %></td>
      <td><%= company.address %></td>
      <td><%= company.number_of_users %></td>
      <td>
        <%= link_to "View", client_company_path(company), :class => "btn btn-outline-success" %>
        <%= link_to "Edit", edit_client_company_path(company), :class => "btn btn-outline-info" %>
        <%= link_to "Delete", client_company_path(company), :class => "btn btn-outline-danger", method: :delete %>
      </td>
    </tr>
  <% end %>

  </tbody>
</table>
